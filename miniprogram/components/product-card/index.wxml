<view class="product-card" bindtap="onTap">
  <image wx:if="{{product.cover_image}}" class="cover" src="{{product.cover_image}}" mode="aspectFill"></image>
  <view wx:else class="cover placeholder">
    <view class="cover-text">{{coverText}}</view>
    <view class="cover-tag">{{conditionText}}</view>
  </view>
  <view class="info">
    <view class="name">{{product.title || '未命名商品'}}</view>
    <view class="desc">{{product.description || '校内见面交易，可现场验货'}}</view>
    <view class="tags">
      <text class="tag">{{campusText}}</text>
      <text class="tag">库存 {{product.stock || 0}}</text>
      <status-badge status="{{product.status || 'on_sale'}}"></status-badge>
    </view>
    <view class="bottom">
      <text class="price">￥{{product.price || '0.00'}}</text>
      <text class="seller">{{product.seller ? product.seller.nickname : '校内同学'}}</text>
    </view>
  </view>
</view>
